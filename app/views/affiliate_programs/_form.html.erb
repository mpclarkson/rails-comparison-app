<%= form_for(@affiliate_program) do |f| %>
  <% if @affiliate_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliate_program.errors.count, "error") %> prohibited this affiliate_program from being saved:</h2>

      <ul>
      <% @affiliate_program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :display_name %><br>
    <%= f.text_field :display_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
